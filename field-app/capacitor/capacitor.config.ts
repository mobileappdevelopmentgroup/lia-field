import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mobileappdevelopmentgroup.liafield',
  appName: 'Lia Field',
  webDir: 'www'
};

export default config;
